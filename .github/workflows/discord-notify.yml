name: Discord Notifications

on:
  push:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
               -X POST \
               -d '{"content":"🚀 New commit pushed to GitHub!"}' \
               $DISCORD_WEBHOOK
